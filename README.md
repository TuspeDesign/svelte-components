# Tuspe SvelteKit Components

Tuspe Design builds websites and online stores for small and large businesses. SvelteKit or React is used as the frontend. This component library contains the necessary elements for using forms and images. The component library supports the colors used by Tailwind, such as `--color-primary` and `--color-gray-600`.
